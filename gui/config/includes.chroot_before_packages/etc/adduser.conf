# netdev NetworkManager without root
# the rest mirror Debian's interactive default set plus the
# hardware-access groups for this board (gpio/i2c/spi).
ADD_EXTRA_GROUPS=1
EXTRA_GROUPS="adm netdev dialout plugdev audio video input render users bluetooth gpio i2c spi"
