#!/bin/sh -e
# DUPLICATE: identical copy in the gui & pocketchip flavors -- keep these in sync (no common/ dir; see README).

# # use greetd for display manager with agreety
# # check config in includes.chroot_after_packages/etc/greetd/config.toml
# rm /etc/greetd/config.toml
# 
# # todo, check if these are necessary
# systemctl set-default graphical.target
# systemctl enable greetd